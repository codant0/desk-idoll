flask>=2.0.0
flask-cors>=3.0.0
Pillow>=8.0.0
numpy>=1.19.0

# AnimatedDrawings (可选，用于高级 AI 动画生成)
# 需要 PyTorch、mmpose 等复杂依赖，建议在独立环境中安装。
# 安装命令：
#   pip install git+https://github.com/facebookresearch/AnimatedDrawings.git
#   torch>=1.9.0
#   torchvision>=0.10.0
#
# 若未安装 AnimatedDrawings，服务会自动降级为基于 Pillow 图像变换的动画生成。
